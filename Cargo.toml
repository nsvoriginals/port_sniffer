[package]
name = "port_sniffer"
version = "0.1.0"
edition = "2021"

[dependencies]
bpaf = { version = "0.9", features = ["derive"] } 
futures = "0.3.32"
tokio = { version = "1.49.0", features = ["full"] }
